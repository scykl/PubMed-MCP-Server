# NCBI API Key (可选，申请地址: https://www.ncbi.nlm.nih.gov/account/settings/)
NCBI_API_KEY=

# MCP 传输协议: stdio 或 sse (默认为 stdio，docker-compose 默认使用 sse)
MCP_TRANSPORT=sse

# SSE 服务监听配置
MCP_HOST=0.0.0.0
MCP_PORT=8090
